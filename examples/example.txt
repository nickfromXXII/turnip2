function bar() : string {
    return "bar";
}

function main() : int {
    var foo: string = "foo";
    var b: bool = foo is "foo";

    if (b) {
        println foo + bar();
    }

    if (foo + bar() >= "foo") {
        println bar();
    }
}
